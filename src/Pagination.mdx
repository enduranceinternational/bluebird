import {
  Pager, Pagination,
} from 'react-bootstrap';
import { PropsTable } from 'docz';
import { Playground } from 'docz';
import PlayGroundCustomProps from 'CustomComponents/PlayGroundCustomProps';

<PlayGroundCustomProps />

# Pagination

## Pager
centers by default

<Playground>
  <Pager>
    <Pager.Item href="#">Previous</Pager.Item>{' '}
    <Pager.Item href="#">Next</Pager.Item>
  </Pager>
</Playground>

## Aligned

<Playground>
  <Pager>
    <Pager.Item previous href="#">
      &larr; Previous Page
    </Pager.Item>
    <Pager.Item next href="#">
      Next Page &rarr;
    </Pager.Item>
  </Pager>
</Playground>

## Disabled

Set the disabled prop to true to disable the link.

<Playground>
  <Pager>
    <Pager.Item previous href="#">
      &larr; Previous
    </Pager.Item>
    <Pager.Item disabled next href="#">
      Next &rarr;
    </Pager.Item>
  </Pager>
</Playground>

## Pagination

<Playground>
  {() => {
    let active = 7;
    let items = [];
    for (let number = 1; number <= 10; number++) {
      items.push(
        <Pagination.Item key={number + '-key'} active={number === active}>{number}</Pagination.Item>
      );
    }

    const paginationBasic = (
      <div>
        <Pagination bsSize="large">{items}</Pagination>
        <br />

        <Pagination bsSize="medium">{items}</Pagination>
        <br />

        <Pagination bsSize="small">{items}</Pagination>
      </div>
    );

    return (paginationBasic);
  }}
</Playground>

## Pagination sub-components

`Ellipsis`, `First`, `Previous`, `Next`, `Last` and `active` prop

<Playground>
  <Pagination>
    <Pagination.First />
    <Pagination.Prev />
    <Pagination.Item>{1}</Pagination.Item>
    <Pagination.Ellipsis />

    <Pagination.Item>{10}</Pagination.Item>
    <Pagination.Item>{11}</Pagination.Item>
    <Pagination.Item active>{12}</Pagination.Item>
    <Pagination.Item>{13}</Pagination.Item>
    <Pagination.Item disabled>{14}</Pagination.Item>

    <Pagination.Ellipsis />
    <Pagination.Item>{20}</Pagination.Item>
    <Pagination.Next />
    <Pagination.Last />
  </Pagination>
</Playground>
